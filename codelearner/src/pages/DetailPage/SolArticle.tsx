import React from "react";
import { useParams } from "react-router";

const SolArticle: React.FC = () => {
  const { solution_article_id } = useParams();
  return <div></div>;
};

export default SolArticle;
