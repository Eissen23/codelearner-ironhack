export type DefaultQueryParam = {
  keyword: string;
  sort: string;
  page: number;
  per_page: number;
};

export type ContentQuery = {
  keyword: string;
  sort: string;
  page: number;
  per_page: number;
  tagged: string;
};
